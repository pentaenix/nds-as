.venv/
__pycache__/
*.pyc
.DS_Store
/dist/
/build/
*.egg-info/
preview_output/
extracted_assets/
converted_glb/

# ROM dumps — never commit game files (keeps folder placeholders only)
roms/**
!roms/README.md
!roms/.gitkeep

# Exported / converted assets — local output only
exports/**
!exports/README.md
!exports/.gitkeep

saves/*
!saves/README.md
!saves/.gitkeep

# Vendored apicula — source is tracked; Rust build output stays local
tools/apicula/target/
