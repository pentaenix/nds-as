# Nintendo DS mappings

Community mapping seeds for `.nds` ROMs. Loaded automatically when RAE scans a Nintendo DS dump.

Add new games here via pull request. Include `platform: "nds"` in each JSON file (optional if the file already lives in this folder).
