# roms/

Put your own legally dumped ROM files here for local RAE use.

Nintendo DS (`.nds`) is supported today. Other extensions will be supported as platform modules land under `src/rae/platforms/`.

Do not commit ROM files to git.
