# Device Toolkit menu repair

This patch restores the top-level **Device Toolkit → Mobile → Extract Installed App ROM…** menu hook.

The action creates generic mobile `.rom` directory bundles under `roms/`, for example `roms/pokemon_home.rom`.
Open those through the normal **File → Open ROM** flow.
